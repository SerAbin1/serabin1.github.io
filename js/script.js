document.addEventListener('DOMContentLoaded', () => {
    // The smooth scrolling and snapping is now handled by CSS scroll-snap.
    // No custom JavaScript is needed for this functionality.

    // For the projects carousel, we'll rely on CSS scroll-snap for now.
    // If more complex carousel functionality is needed (e.g., buttons, indicators),
    // it can be added here later.
});